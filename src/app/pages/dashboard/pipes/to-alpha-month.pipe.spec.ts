import { ToAlphaMonthPipe } from './to-alpha-month.pipe';

describe('ToAlphaMonthPipe', () => {
  it('create an instance', () => {
    const pipe = new ToAlphaMonthPipe();
    expect(pipe).toBeTruthy();
  });
});
